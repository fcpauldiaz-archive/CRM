{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Producto</h1>

    <table class="table table-striped table-hover">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.0.id }}</td>
            </tr>
            <tr>
                <th>Producto</th>
                <td>{{ entity.0.producto }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a class="btn btn-primary "href="{{ path('producto') }}">
            Ver todos
        </a>
    </li>
    <li>
        <a class="btn btn-danger" href="{{ path('producto_edit', { 'id': entity.0.id }) }}">
            Editar
        </a>
    </li>
      <li>
        <a class="btn btn-primary" href="{{ path('producto_new') }}">
            CREAR OTRO PRODUCTO
        </a>
    </li>
    
</ul>
{% endblock %}