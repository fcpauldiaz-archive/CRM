{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Listado de Direcciones</h1>

    <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th>Id</th>
                <th>Direccion</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('correo_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.direccion }}</td>
                <td>
                <ul>
                    <li>
                        <a class ="btn btn-xs btn-info "href="{{ path('direccion_show', { 'id': entity.id }) }}">Mostrar</a>
                    </li>
                    <li>
                        <a class="btn btn-xs btn-danger" href="{{ path('direccion_edit', { 'id': entity.id }) }}">Editar</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a class="btn btn-primary btn-block" href="{{ path('direccion_new') }}">
               Crear nueva Direccion
            </a>
        </li>
    </ul>
    {% endblock %}