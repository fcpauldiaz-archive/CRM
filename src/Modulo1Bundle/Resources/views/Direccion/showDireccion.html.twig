{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Direccion</h1>

    <table class="table table-striped table-hover">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.0.id }}</td>
            </tr>
            <tr>
                <th>Direccion</th>
                <td>{{ entity.0.direccion }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a class="btn btn-primary "href="{{ path('direccion') }}">
            Ver todos
        </a>
    </li>
    <li>
        <a class="btn btn-danger" href="{{ path('correo_edit', { 'id': entity.0.id }) }}">
            Editar
        </a>
    </li>
    
</ul>
{% endblock %}