{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Crear Venta</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('ventas') }}">
            Regresar
        </a>
    </li>
</ul>
{% endblock %}