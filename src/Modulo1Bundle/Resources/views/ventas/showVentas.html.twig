{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Ventas</h1>

    <table class="table table-striped table-hover">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.0.id }}</td>
            </tr>
            <tr>
                <th>Total de venta</th>
                <td>{{ entity.0.total }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a class="btn btn-primary "href="{{ path('venta') }}">
            Ver todos
        </a>
    </li>
    <li>
        <a class="btn btn-danger" href="{{ path('venta_edit', { 'id': entity.0.id }) }}">
            Editar
        </a>
    </li>
    
</ul>
{% endblock %}