{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Ventas</h1>

    <table class="table table-striped table-hover">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.0.id }}</td>
            </tr>
             <tr>
                <th>Producto</th>
                <td>{{ producto}}</td>
            </tr>
             <tr>
                <th>Cantidad</th>
                <td>{{ entity.0.cantidad }}</td>
            </tr>
            <tr>
                <th>Total de venta</th>
                <td>{{ entity.0.total }}</td>
            </tr>
              <tr>
                <th>Cliente</th>
                <td>{{ cliente}}</td>
            </tr>
               <tr>
                <th>Fecha</th>
                <td>{{ entity.0.fecha}}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a class="btn btn-primary "href="{{ path('ventas') }}">
            Ver todos
        </a>
    </li>
    <li>
        <a class="btn btn-danger" href="{{ path('venta_edit', { 'id': entity.0.id }) }}">
            Editar
        </a>
    </li>
    
</ul>
{% endblock %}