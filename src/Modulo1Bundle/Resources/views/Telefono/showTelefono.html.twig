{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Telefono</h1>

    <table class="table table-striped table-hover">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.0.id }}</td>
            </tr>
            <tr>
                <th>Telefono</th>
                <td>{{ entity.0.numero_telefono }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a class="btn btn-primary "href="{{ path('telefono') }}">
            Ver todos
        </a>
    </li>
    <li>
        <a class="btn btn-danger" href="{{ path('telefono_edit', { 'id': entity.0.id }) }}">
            Editar
        </a>
    </li>
    
</ul>
{% endblock %}