{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Crear Cliente</h1>

    {{ form(form) }}

    <ul class="record_actions">
    <li>
        {#<a href="{{ path('cliente') }}">
            Ver todos
        </a>#}
    </li>
</ul>
{% endblock %}
