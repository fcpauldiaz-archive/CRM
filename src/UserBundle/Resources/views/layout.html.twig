{% extends '::base.html.twig' %}

    {% block manual_style %}
        <style>
        .btn span.glyphicon {               
            opacity: 0;             
        }
        .btn.active span.glyphicon {                
            opacity: 1;             
        }
    </style>
{% endblock %}
    
    {% block body %}
        
     
        
        <br>
        <br>
        <br>
        
        <!-- Start Main Body Section -->
        <div class="mainbody-section text-center">
            <div class="container">
                <div class="row">
                    
                    <div class="col-md-3">
                        
                       
                        
                       
                        
                    </div>
                    
                    <div class="col-md-6">
                        
    <!-- Custom Section -->

    <div style = "color: #ffffff">
            {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                {{ 'layout.logged_in_as'|trans({'%username%': app.user.username}, 'FOSUserBundle') }} |
                <a href="{{ path('fos_user_security_logout') }}">
                    {{ 'layout.logout'|trans({}, 'FOSUserBundle') }}
                </a>
            {% else %}
                
            {% endif %}
        </div>

        {% for type, messages in app.session.flashBag.all %}
            {% for message in messages %}
                <div class="{{ type }}">
                    <p style="border-bottom: 1px solid #ffffff; color: #ffffff "> {{ message|trans({}, 'FOSUserBundle') }}</p>
                </div>
            {% endfor %}
        {% endfor %}

      
    <!-- Start Login Section -->
    
    {% block fos_user_content %}
    {% endblock fos_user_content %}
       
    <!-- End Custom Section -->
                        
                        <div class="row">
                          
                            
                        </div>
                        
                    </div>
                    
                    <div class="col-md-3">
                        
                       
                        
                      
                        
                       
                        
                    </div>
                </div>
            </div>
        </div>
        <!-- End Main Body Section -->
        
        <footer>
            <!-- Start Copyright Section -->
            <div class="copyright text-center">
                <div class="container">
                    <div class="row">
                        <div class="col-md-12">
                            <div>CRM<a href="#" target="_blank"></a> <a href="https://facebook.com/newtonlabsgt" target="_blank">Newton Labs</a> </div>
                        </div>
                    </div>
                </div>
            </div>
            <!-- End Copyright Section -->
        </footer>
        
       
        
        
        
    {% endblock body %}       
