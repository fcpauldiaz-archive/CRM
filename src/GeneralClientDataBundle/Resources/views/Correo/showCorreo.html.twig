{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Correo</h1>

    <table class="table table-striped table-hover">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Correo electrónico</th>
                <td>{{ entity.correoElectronico }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a class="btn btn-primary "href="{{ path('correo') }}">
            Ver todos
        </a>
    </li>
    <li>
        <a class="btn btn-danger" href="{{ path('correo_edit', { 'id': entity.id }) }}">
            Editar
        </a>
    </li>
    
</ul>
{% endblock %}
